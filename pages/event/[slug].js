import React from 'react';
import { useRouter } from 'next/router';

const Slug = () => {
    const router = useRouter();
    return (
        <div>
            Slug Function
        </div>
    );
}

export default Slug;
